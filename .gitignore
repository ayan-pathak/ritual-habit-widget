# Build output
build/
app/build/
.gradle/
*.apk
*.aab

# Local machine config — points at this machine's SDK
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Design working artifacts (the seeded canvases are large and regenerable)
design/*.html
design2/*.html

# Kotlin compiler scratch
.kotlin/

# Bytecode cache from tools/mochi.py
__pycache__/
google-services.json

# The Play upload key, when CI writes it from a secret. Never checked in.
keystore/upload.jks
